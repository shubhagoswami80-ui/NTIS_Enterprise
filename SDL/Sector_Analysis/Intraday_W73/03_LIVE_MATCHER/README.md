# Live Matcher

The initial matcher lives in `strategy.py` and is intentionally conservative.

Promotion requirement:
- exact V8-compatible current snapshot
- exact frozen W73 condition set
- no silent coercion of missing values into zeros
