# Local copied research source

Any previous-analysis code required by W73 must be copied into this folder and
adapted locally. W73 must import the local copy, never the old source.

Authoritative chain to preserve:
- V8 discovery semantics
- V12.5 exact-V8 stability
- V12.6 exhaustive miner
- V12.6 corrected validation
- exact raw-data feature adapter used by the strategy

Only code actually used by W73 should be retained.
