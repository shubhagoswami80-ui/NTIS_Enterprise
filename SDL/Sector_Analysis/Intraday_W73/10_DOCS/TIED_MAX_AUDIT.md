# Tied maximum audit

The extraction found TWO candidates tied at 72.093%. Both are preserved.
W73 will not silently choose the first candidate.

Run the audit against the W73-owned baseline. The output identifies common and
differing conditions. Then the feature engine can implement two explicit setup
variants if necessary. No condition is relaxed to increase live matches.
