# W73 Dashboard Restoration v1

Restores the previously validated rich W73 trader-board UI (`v9`) that was lost when the consolidated deployment replaced the dashboard with a minimal UI.

Only `11_DASHBOARD/w73_dashboard.py` is replaced. No backend, V8 strategy, live decision service, source adapter, cache, integration boundary, or SDL production code is changed.
